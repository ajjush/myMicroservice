name: CI + CD

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Compile
        run: echo Hello, world!
      - name: Fail
        run: exit 1

